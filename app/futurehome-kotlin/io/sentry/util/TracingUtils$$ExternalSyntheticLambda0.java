package io.sentry.util;

import io.sentry.IScope;
import io.sentry.ScopeCallback;

// $VF: synthetic class
public final class TracingUtils$$ExternalSyntheticLambda0 implements ScopeCallback {
   @Override
   public final void run(IScope var1) {
      TracingUtils.lambda$startNewTrace$1(var1);
   }
}
